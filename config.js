// Paste your two Supabase values here (Supabase → Project Settings → API / Data API).
// Leave them as they are to run the game offline-only (no accounts, no public library).
window.CR_CONFIG = {
  supabaseUrl: "https://YOUR-PROJECT.supabase.co",
  supabaseAnonKey: "YOUR-ANON-OR-PUBLISHABLE-KEY"
};
